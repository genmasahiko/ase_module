from setuptools import setup, find_packages

setup(
    name="ase_mymodule",
    version="0.1",
    description="Tools for ASE",
    author="Masahiko Gen",
    packages=find_packages(),
    install_requires=[],
)
